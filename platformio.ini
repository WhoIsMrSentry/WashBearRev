[env:uno]
platform = atmelavr
board = uno
framework = arduino
monitor_speed = 115200

# Recommended libraries (use PlatformIO Library Registry names or install via Library Manager):
# lib_deps =
#   i2cdevlib (I2Cdev)
#   electroniccats/MPU6050@^1.0.2
